%% sim-01: Unit Conversion Functions

function F = c2f(C)
    % TODO: Celsius to Fahrenheit
    F = C;
end

function bar_val = psi2bar(psi)
    % TODO: 1 bar = 14.5038 psi
    bar_val = psi;
end

function gpm = kgs2gpm(kgs)
    % TODO: kg/s to gallons per minute (water)
    gpm = kgs;
end
