%% sim-04: Compare P-only, PI, PID responses
% TODO: Plot three closed-loop step responses overlaid
% G = tf(1, [10 1]);
% C_p  = pid(5, 0, 0);
% C_pi = pid(5, 0.5, 0);
% C_pid = pid(5, 0.5, 2);
% T_p  = feedback(C_p*G, 1);
% T_pi = feedback(C_pi*G, 1);
% T_pid = feedback(C_pid*G, 1);
% step(T_p, T_pi, T_pid, 50); legend('P','PI','PID'); grid on;
