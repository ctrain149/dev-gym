/** oop-04: TODO — Triggers emergency shutdown on EMERGENCY severity. */
public class ShutdownObserver implements AlarmObserver {
    @Override
    public void onAlarm(AlarmEvent event) {
        // TODO: if EMERGENCY, print "!!! EMERGENCY SHUTDOWN INITIATED !!!"
    }
}
