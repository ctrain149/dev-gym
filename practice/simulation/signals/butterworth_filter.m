%% sim-02: Butterworth Low-Pass Filter
% TODO:
% [b, a] = butter(4, 0.1);  % 4th order, 0.1 normalized cutoff
% filtered = filter(b, a, raw);
% Compute SNR before and after: snr = 10*log10(var(signal)/var(noise));
